{% extends 'base.html.twig' %}

{% block title %}Nous Contacter{% endblock %}

{% block body %}

    {{ form_start(form) }}
    <div class="container-fluid formBackG">
        <div class="row justify-content-center">
            <div class="col-12 col-md-4 form-group">
                <h3 class="formTitle mb-5 mt-4 text-center"> Contacer nous</h3>

    <div class="form-group">{{ form_row(form.name, {'attr':{'class':'form-control mb-2 mt-2 formInput' }}) }}</div>
    <div class="form-group">{{ form_row(form.email, {'attr':{'class':'form-control mb-2 mt-2 formInput'}}) }}</div>
    <div class="form-group">{{ form_row(form.messageContact, {'attr':{'class':'form-control mb-2 mt-2 formInput'}}) }}</div>

	<button class="btn mb-3 mt-3 formButton" type="submit">Ajouter</button>
    {{ form_end(form) }}
    		</div>
		</div>
	</div>

{% endblock %}
